libfdt++
========

* github CI task for clang
* github CI task for MSVC
* property set<T> like as<T>
* return a range from as_stringlist (transform? filter?)
* memory reservation block
* boot cpuid
* inplace load
  - we could avoid making copies of the FDT data by storing & returning
    references to the FDT memory.

libfit++
========

* support creating FIT images
* support modifying FIT images
* support writing FIT images
